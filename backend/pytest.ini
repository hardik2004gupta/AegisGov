[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    opa: requires a running OPA server on localhost:8181
    db: requires a running PostgreSQL instance
    integration: requires both OPA and PostgreSQL (opa + db)
